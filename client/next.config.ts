import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
    /* config options here */
    images: {
        domains: ['lh3.googleusercontent.com'], // add any external image domains you need
    },
};

export default nextConfig;
